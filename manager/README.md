# Manager Folder Notes

- Active manager page: `/manager.html` (repo root).
- Legacy prototype page: `/manager/manager.html` (kept for reference only).
- Shared manager bottleneck helpers: `/manager/bottleneck.py`.

## Why this exists

There are two similarly named manager HTML files in this repo. This note avoids
accidental edits to the legacy prototype when changing the live manager UI.
